[project]
name = "agents-for-humans"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.141.1",
    "strands-agents[litellm]>=1.53.0",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
